## Agent skills

### Issue tracker

GitHub issues in the repo (github.com/Queracus/python-snake.git). Uses the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout via `CONTEXT-MAP.md` at repo root. See `docs/agents/domain.md`.